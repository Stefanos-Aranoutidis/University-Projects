<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="16dp"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/tvDate"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textStyle="bold" android:textSize="18sp" android:textColor="@color/text_primary"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPickDate"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Επιλογή ημερομηνίας" android:layout_marginTop="8dp"
            app:backgroundTint="@color/button_red" android:textColor="@android:color/white"/>

        <EditText
            android:id="@+id/etAmount"
            android:layout_width="260dp" android:layout_height="wrap_content"
            android:hint="Ποσό σε € (π.χ. 9.90)"
            android:inputType="numberDecimal" android:layout_marginTop="16dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSave"
            android:layout_width="260dp" android:layout_height="wrap_content"
            android:text="Αποθήκευση" android:layout_marginTop="16dp"
            app:backgroundTint="@color/button_red" android:textColor="@android:color/white"/>
    </LinearLayout>
</ScrollView>
